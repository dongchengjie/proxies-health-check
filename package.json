{
  "name": "proxies-health-check",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run ./index.ts",
    "build": "docker build --pull --rm -f Dockerfile -t proxies-health-check:latest .",
    "serve": "docker run -it --rm -p 3000:3000 -p 9090:9090 proxies-health-check:latest"
  },
  "devDependencies": {
    "@actions/core": "^1.11.1",
    "@types/bun": "^1.3.2",
    "@types/fs-extra": "^11.0.4",
    "axios": "^1.13.2",
    "fs-extra": "^11.3.2",
    "nanoid": "^5.1.6",
    "p-limit": "^7.2.0",
    "typescript": "^5.0.0",
    "yaml": "^2.8.1"
  }
}
